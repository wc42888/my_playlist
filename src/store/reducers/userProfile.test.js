import * as actions from '../actions/userProfile';

describe('test user profile reducer', () => {
  describe(`on ${actions.GET_USER_PROFILE_SUCCESS}`, () => {
    it('should updated the user profile correctly', () => {
      expect(true).toBeTruthy();
    });
  });
});
